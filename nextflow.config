profiles {

    standard {
        workDir = "work"
        resume = true  // null for no resume
        docker {
            enabled = true
        }
        process {
            executor = 'local'
        }
    }
}